# 2.0 Features

Backlog of feature ideas not yet scheduled for implementation. Each entry
should get its own detailed plan (see the `plan-feature` skill) when work on
it actually starts.

## Bug: Ship Turn/Thrust Rate Tied to Render Frame Rate

Reported symptom: the spaceship sometimes feels sluggish to turn/accelerate
and other times feels fluid, seemingly at random.

Root cause: `gameTick` (`src/index.js`) integrates position through a
fixed-timestep accumulator loop —

```js
while (accumulator >= dt) {
  updatePhysics();
  accumulator -= dt;
}
```

— so the number of `updatePhysics()` steps per real second is frame-rate
independent, and coasting/drift (movement with no input) tracks real
elapsed time correctly regardless of `requestAnimationFrame` rate.

`checkInput()` (`src/index.js`, called once per `gameTick`, i.e. once per
rAF callback, *outside* that loop) does not have the same property:

```js
spaceship.rotate(-2);       // flat 2° every call — not scaled by dt or elapsed time
spaceship.accelerate(dt);   // uses the fixed dt constant, but applied once per call
```

Both effectively scale with `frames-per-second`, not real time: turn rate
is `2° × FPS`, thrust ramp-up is `(accelerate's dt-scaled delta) × FPS`.
When the browser's rAF rate dips (background-tab throttling, a GC pause, a
slower display refresh, dev tools open, etc.), turning and accelerating
become noticeably sluggish even though drift/coasting keeps tracking real
time — producing the reported "sometimes slow, sometimes fluid" feel.

**Proposed fix**: move the rotate/accelerate/decelerate calls into the
fixed-`dt` loop — poll `keys[...]` from inside `updatePhysics()` instead of
once in `checkInput()` — so they run a frame-rate-independent number of
times per real second, same as position updates already do. The rest of
`checkInput` (pause toggle, shooting cooldown, difficulty-select edge
detection) is already gated by `performance.now()` deltas or the
`pressed[]` one-shot latch, so it can stay called once per rAF callback.

## Decouple Render Frame Rate from Simulation Tick Rate

Related to, but distinct from, the input bug above: that fix makes
`checkInput` run at the fixed simulation rate instead of once per render
frame. This entry is about the fixed-timestep loop itself
(`gameTick`/`updatePhysics`, `src/index.js`), which has two gaps that
matter once frame rate and simulation rate diverge further than "occasional
dip":

- **No interpolation between ticks.** `renderGraphics()` runs once per
  `requestAnimationFrame` callback, after the `while (accumulator >= dt)`
  loop has drained as many whole `dt` steps as fit — it always draws the
  state as of the last completed tick, never a position between two ticks.
  On a high-refresh display (rAF faster than the simulation's implied
  ~100 Hz, since `dt = 0.01`), some rAF callbacks accumulate less than one
  `dt` and do zero physics steps, so consecutive frames render the exact
  same positions — motion looks like it's running below the display's
  actual refresh rate even though the simulation itself is correct.
  Standard fix: keep the previous tick's state alongside the current one,
  and interpolate between them at render time using
  `alpha = accumulator / dt`.
- **No catch-up cap ("spiral of death").** `updateTiming` (`src/index.js`)
  feeds `accumulator += elapsed` with no upper bound on `elapsed`. If a
  frame takes unusually long — tab backgrounded then foregrounded, a long
  GC pause, dev tools opened mid-frame — the next `gameTick` can see a
  large `elapsed`, queue many `updatePhysics()` iterations to catch up, and
  if that catch-up itself takes longer than one frame budget, the *next*
  `elapsed` is even larger, compounding indefinitely. Standard fix: clamp
  `elapsed` (or the accumulator) to a maximum per-frame value, accepting a
  brief slowdown in simulated time rather than a runaway loop.

Both are textbook parts of the "fixed update + variable render" pattern
this loop already mostly implements; this entry is about closing the two
gaps rather than introducing the pattern from scratch.

### Gameplay Logic Also Keyed Off the Render-Frame Counter

Separately from the loop mechanics above, several gameplay rules use
`frame` (`src/index.js`) — a counter incremented once per non-paused
`gameTick`, i.e. once per `requestAnimationFrame` callback, wrapping at
`GAME_FPS` (60) — as a stand-in for "one real second," rather than
deriving from the fixed-`dt` simulation clock or `performance.now()` the
way `checkInput`'s pause/shoot cooldowns already correctly do. All of
these drift proportionally to however far the actual rAF rate is from
60 Hz (faster on a 120/144 Hz display, slower under tab-throttling or a
slow device):

- **Per-second score increment** — `if (frame == 60) score++;`
  (`updateGame`, `src/index.js`). Score accrues in step with render frame
  rate, not elapsed wall time.
- **New life check** — `if (score % 50 == 0 && frame == 60) lives++;`
  (`updateGame`, `src/index.js`, right below the score increment). Inherits
  the same drift, since it's gated on both `frame == 60` and the
  already-frame-rate-coupled `score`.
- **Asteroid spawn cadence** — `if (asteroids.length < Math.log(score + 1)
  && frame % 30 == 0) spawnAsteroid();` (`updatePhysics`, `src/index.js`).
  This one is worse than pacing drift: `updatePhysics` runs inside the
  fixed-`dt` `while (accumulator >= dt)` loop and can execute multiple
  times per `gameTick`, but `frame` only changes once per `gameTick` — so
  during a catch-up burst (see the spiral-of-death gap above), `frame % 30
  == 0` stays true across every iteration of that inner loop and
  `spawnAsteroid()` fires once per physics step instead of once per
  render frame, spawning a burst of asteroids in a single lag spike rather
  than one.
- **Bullet lifetime/expiry** — `Spaceship.shoot(frame)` stores the raw
  wrapping `frame` value on the bullet (`src/models/spaceship.js`,
  `src/models/bullet.js` constructor); `Bullet.checkDistance(frame)`
  (`src/models/bullet.js`) only increments the bullet's internal `time`
  when the *current* wrapping counter happens to equal the value stored at
  spawn — i.e., once per ~60-frame lap — and expires the bullet after two
  such laps. Bullet range is therefore also tied to render frame rate, and
  fragile independent of that: it compares raw counter values rather than
  elapsed laps or time, so it would break if `GAME_FPS` or the wrap point
  ever changed.

Proposed fix: replace all four with time derived from the simulation
clock already available in the fixed-`dt` loop (e.g. an accumulated
simulated-seconds counter, or a tick count incremented once per
`updatePhysics()` call rather than once per `gameTick`) instead of the
render-frame counter — the same fix direction as the input-coupling bug
above, applied to scoring/spawning/lifetime instead of steering.

## Instructions on Start Screen

Move the current static instructions text (in the `index.html` wrapper below
the canvas: controls for movement, shooting, pause) into the in-game start
screen shown by `drawMessage`/`canvas.drawPaused` (`src/index.js`,
`src/lib/canvas.js`) instead of, or in addition to, the page body. Add a way
to view them again without restarting:

- A button/key on the start (new-game/difficulty-select) screen.
- A button/key in the pause menu to re-open the instructions overlay.

Needs a way to distinguish "instructions" state from "paused" and
"choose difficulty" state in the existing `paused`/`newGame` flags, or a new
state variable, plus canvas rendering for the instructions text itself
(currently only in HTML/CSS, not drawn on canvas).

## Settings Menu (Sound Toggle, Volume, High Score Reset)

Add a settings menu/panel with:

- Sound on/off toggle, wired to `Sound` (`src/lib/sound.js`) — likely a
  mute flag checked in `play()`, or setting `volume` to 0.
- Volume slider, replacing the hardcoded per-effect volumes currently passed
  into `new Sound(src, vol)` in `src/index.js` (e.g. `0.1` for
  `pewSound`, `0.25` for `boomSound`) with a shared, adjustable value.
- Relocate the "Reset High Score" button/dialog (`#reset-high`,
  `#reset-high-dialog` in `index.html`) here instead of the page body, so it
  lives alongside other game settings.

Likely reuses the `<dialog>` pattern established for the high-score reset
confirmation.

## Generic Entity Draw Interface

`Canvas` (`src/lib/canvas.js`) currently has one draw method per entity
type — `drawSpaceship`, `drawAsteroids`, `drawBullets` — each hardcoding
that entity's shape math. Every new entity type means another Canvas
method. Instead, give each entity a method (e.g. `getDrawShape()`;
`Asteroid.getBounds()` already returns something close to this) that
returns drawing primitives, and have `Canvas` iterate/execute them
generically instead of dispatching per type.

Keep the actual `context` calls (transform save/restore, fill/stroke) in
`Canvas`, not on the entity classes — the model classes
(`Entity`/`Asteroid`/`Bullet`/`Spaceship`) are intentionally free of any
DOM/canvas dependency today, which is what let them be unit-tested
directly with no rendering mocks (see [repo-modernization Phase
6](repo-modernization/phase-6-tooling-testing.md)). Moving `draw()` calls
onto the models themselves would lose that.

The five HUD draw methods (`drawScore`, `drawHigh`, `drawLives`,
`drawPaused`, `drawDiff`) aren't tied to a single entity and would stay on
`Canvas` as-is.

## Rotation/Thrust for Other Entities

`Entity` (`src/models/entity.js`) keeps `orientation`, `rotate()`, and
`orientationToRadians()` on the base class even though only `Spaceship` uses
them today — `jerk`, `maxD`, `accelerate()`, and `decelerate()` were moved
onto `Spaceship` itself since `Asteroid`/`Bullet` never steer. The three
base-class members were deliberately kept in case a future entity (e.g. a
steerable enemy ship, a homing asteroid) needs rotation without needing full
thrust/acceleration. If that never materializes, `orientation`/`rotate()`
could move to `Spaceship` too, leaving `Entity` as pure position/velocity
kinematics.

## WASM Feasibility Investigation

Whether compiling performance-critical parts of the game to WebAssembly
would meaningfully help. This is evaluation only; a future migration would
be a separate, later effort gated on the findings below. Originally scoped
as a research sub-section of
[repo-modernization Phase 5](repo-modernization/phase-5-performance.md);
moved here since it's forward-looking research gated on a future feature
that doesn't exist yet, not part of that modernization pass.

**Current state**: fixed-timestep loop driven by `requestAnimationFrame` in
`src/index.js` (game tick → input → physics/collision → render). Physics /
collision is simple point-in-AABB checking in `entity.js`
(`checkCollision`); the physics step runs an O(bullets × asteroids) nested
loop every frame for bullet/asteroid hits, plus an O(asteroids) loop for
ship/asteroid hits. Entity counts are intentionally small — asteroid spawn
is capped by `asteroids.length < Math.log(score)`, so the simulation holds
on the order of tens of entities even at a high score, not hundreds. **No
existing performance problem is documented** — no profiling data, no
frame-drop reports, no perf issues on file; this is forward-looking, not a
fix for a known regression.

- [ ] Capture a performance baseline: Chrome DevTools Performance traces
      during normal play (steady state, and a "worst case" with max
      asteroids/bullets on screen at highest difficulty) — note frame time,
      scripting time, GC pauses.
- [ ] Identify and rank candidate hot paths from the trace plus code
      reading: the collision nested loop, `Asteroid.genBounds` polygon
      math, `Entity.updatePosition`/`accelerate`/`decelerate` trig calls.
      Note which are actually script-bound vs. already GPU/compositor-bound
      (Canvas2D draw calls themselves won't benefit from WASM — only the
      JS-side math feeding them would).
- [ ] Research WASM toolchain options for this Vite + vanilla JS project
      (see [repo-modernization Phase
      1](repo-modernization/phase-1-dependency-toolchain.md)) with no
      existing TypeScript/Rust tooling: AssemblyScript (TS-like syntax,
      cheap interop, smaller learning curve) vs. Rust +
      `wasm-bindgen`/`wasm-pack` (better codegen, steeper toolchain/CI
      cost) vs. hand-written WAT (not realistic at this project size).
      Vite has native `.wasm` import support via `?init`, or
      `vite-plugin-wasm` for more involved cases.
- [ ] Build a throwaway micro-benchmark comparing JS vs. a WASM
      implementation for the single highest-ranked hot path (most likely
      the bullet/asteroid collision loop or bounds generation), at
      realistic entity counts (tens) and an exaggerated stress count
      (hundreds/thousands) to see where any crossover point is. Measure
      with `performance.now()` across many iterations, not a single call.
- [ ] Evaluate non-performance costs observed while prototyping: JS↔WASM
      call overhead for small/frequent calls (a known WASM weak point at
      this scale), data marshaling cost for entity arrays, added bundle
      size, added build complexity, debuggability/DX regression vs. plain
      JS.
- [ ] Write up findings as a recommendation appended to this section:
      whether WASM is worth adopting now, what numeric threshold (entity
      count, frame budget) would justify it, which hot path(s) would be the
      first candidate if revisited, and which toolchain to prefer if/when
      that happens.
- [ ] Delete or clearly mark any prototype/benchmark code as scratch so it
      doesn't get mistaken for shipped functionality.

**Open questions:**

- Is there an actual observed performance problem today, or is the WASM
  question purely forward-looking? No profiling data currently exists — the
  baseline capture step above should answer this before further investment.
- What future feature would actually push entity/compute counts up enough
  to matter (denser asteroid fields, particle effects, larger canvas,
  multiplayer/netcode prediction)? Without one of these, WASM adoption is
  likely premature for a game of this scope.
- Given the small, frequent calls this codebase would make into WASM
  (per-entity, per-frame), does the JS↔WASM boundary overhead outweigh the
  raw compute savings at realistic entity counts? This needs to come out of
  the micro-benchmark rather than being assumed either way.
- If adopted, does WASM change how the JS-side game loop is structured
  (e.g., moving the whole entity array into WASM-managed linear memory
  instead of calling in/out per entity)? That's a much larger architectural
  change than "port one function" and should be scoped separately if the
  recommendation leans that direction.
